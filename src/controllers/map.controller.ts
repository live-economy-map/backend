import type { Request, Response, NextFunction } from 'express';

export const getCells = async (req: Request, res: Response, next: NextFunction) => {
  next(new Error('Not implemented'));
};

export const getCellDetail = async (req: Request, res: Response, next: NextFunction) => {
  next(new Error('Not implemented'));
};

export const getRawLayer = async (req: Request, res: Response, next: NextFunction) => {
  next(new Error('Not implemented'));
};

export const searchMap = async (req: Request, res: Response, next: NextFunction) => {
  next(new Error('Not implemented'));
};
